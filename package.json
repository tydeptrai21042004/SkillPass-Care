{
  "name": "skillpass-care-pilot",
  "version": "0.4.0",
  "private": true,
  "description": "Vercel-ready pilot for portable service coverage on CKB-style live Cell ownership.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web \"npm run dev -w @skillpass/api\" \"npm run dev -w @skillpass/web\"",
    "build:packages": "npm run build -w @skillpass/shared && npm run build -w @skillpass/core && npm run build -w @skillpass/ckb-adapter && npm run build -w @skillpass/provider-sdk && npm run build -w @skillpass/config",
    "build:apps": "npm run build -w @skillpass/api && npm run build -w @skillpass/web",
    "build": "npm run build:packages && npm run build:apps",
    "build:vercel": "npm run build:packages && npm run build -w @skillpass/web",
    "test": "npm run build:packages && npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present && tsc -p tsconfig.vercel.json --noEmit",
    "check": "npm run preflight && npm run typecheck && npm test && npm run build:apps",
    "preflight": "node scripts/preflight.mjs",
    "product:commitment": "node scripts/product-commitment.mjs"
  },
  "devDependencies": {
    "concurrently": "9.0.1",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "npm@10.9.0",
  "type": "module"
}
