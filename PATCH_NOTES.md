# SkillPass Care v0.4.0 patch

Copy these files over the repository root, then delete every path listed in `REMOVED_FILES.txt`.

This patch contains only files that are new or changed relative to the supplied `SkillPass-Care-main(8).zip`.

Full dependency-backed `npm run check` still needs a networked npm install because registry access was unavailable in the preparation environment. See `CORRECTIONS.md` and `docs/DEPLOYMENT.md`.
