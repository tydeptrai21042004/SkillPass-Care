{
  "name": "@skillpass-care/core",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@skillpass-care/shared": "*"
  },
  "devDependencies": {
    "vitest": "2.1.4"
  }
}
